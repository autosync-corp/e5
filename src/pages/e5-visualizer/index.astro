---
// E5's own custom-built visualizer (ported from the `testground` staging
// repo — see ai/VISUALIZER_MIGRATION_NOTES.md). It's a standalone static app
// with its own full nav bar matching the main site's look, so it's served as
// a real page of its own rather than wrapped in MainLayout (which would
// duplicate the nav/footer around it).
//
// This is a NEW, separate tool from /visualizer (which stays on the AutoSync
// widget). Not linked from anywhere on the site yet — accessible only via
// direct link until the user decides where it should live.
return Astro.redirect('/visualizer-app/e5wheels-visualizer_9.html', 302);
---
