export enum Orientations {
    LEFT = "left",
    RIGHT = "right",
    CENTER = "center",
    NONE = "none",
}

export enum INDEXES {
    ZERO = 0,
    ONE = 1,
    TWO = 2,
    THREE = 3,
    FOUR = 4,
}